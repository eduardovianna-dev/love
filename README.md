# Love — Single Page Web App

Este é um projeto front-end simples desenvolvido em **HTML**, **CSS** e **JavaScript**, criado originalmente como uma página personalizada com animações, interações e um design minimalista.  

---

## 🔧 Tecnologias

- **HTML5** — estrutura da página  
- **CSS3** — responsividade, animações e estilo  
- **JavaScript Vanilla** — manipulação de elementos e lógica de interatividade  
- **Service Worker** — para cache e possível comportamento offline (se já estiver implementado)

---

## 📁 Estrutura do Projeto
├── index.html
├── styles.css
├── app.js
└── service-worker.js
└── manifest.json
└── icon.png
- `index.html` — marcação da página principal  
- `styles.css` — estilos e responsividade  
- `app.js` — scripts para interações e animações  
- `service-worker.js` — lógica para PWA / cache 
- `manifest.json` — configuração para PWA  
- `icon.png` — ícone usado para a app (PWA)

---

## 📱 Funcionalidades

- Layout responsivo para desktop e dispositivos móveis  
- Interações visuais usando JavaScript  
- Animações suaves aplicadas via CSS  
- Suporte a PWA (service worker + manifest), se você já adicionou essa parte

---

## ▶ Como Executar Localmente

1. Clone o repositório:  
   ```bash
   git clone https://github.com/eduardovianna-dev/love.git
2. Abra o index.html no navegador ou use um servidor local (exemplo: Live Server no VSCode ou python -m http.server) para testar o service worker.
3. Senha: bb.



